version: 1
description: 'Proposed split configuration layout generated from data_ibov.yaml. Safe: does not replace config/data.yaml.'
generated_at: '2026-05-10 02:25:04'
source_file: data_ibov.yaml
recommended_files:
  assets: config/assets/ibov_assets.yaml
  universe: config/universes/ibov.yaml
  indices: config/indices/catalog.yaml
  context: config/context/layers.yaml
  sources: config/sources/data_sources.yaml
  defaults: config/defaults/asset_defaults.yaml
principle:
  assets: Only static asset identity and classification.
  universes: Only membership/composition of universes such as IBOV.
  indices: Only benchmark/index catalog.
  context: Only context layers and candidate context tickers.
  sources: Only source/provenance definitions.
  defaults: Only shared defaults.
counts:
  assets: 79
  ibov_components: 79
  asset_context_overrides: 79
